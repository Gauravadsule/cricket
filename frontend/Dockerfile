# Use an official Nginx image to serve the frontend
FROM nginx:alpine

# Copy the frontend HTML file into the Nginx container
COPY frontend/index.html /usr/share/nginx/html/

# Expose port 80 to access the frontend
EXPOSE 80